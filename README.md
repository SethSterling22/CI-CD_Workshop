# CI-CD_Workshop

### Structure:

```
taller-cicd/
├── .github/
│   └── workflows/
│       └── pipeline.yml    # El corazón de la automatización
├── app/
│   ├── main.py             # Aplicación simple
│   └── requirements.txt    # Dependencias
├── Dockerfile              # Instrucciones de imagen
└── .gitignore
```
